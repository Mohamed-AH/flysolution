# Fly.io API Token for authentication
# Generate an organization-scoped token using: fly tokens create org
# This token provides the necessary permissions for the dashboard
FLY_API_TOKEN=your_token_from_fly_tokens_create_org_here